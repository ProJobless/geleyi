/** The theme settings **/


/* --- The slider settings --- */
	
	// The list names of photos from folder "/img/slider/"
	var photos = ['maf_1.jpg', 'fd_1.jpg', 'maf_2.jpg', 'fd_2.jpg'];
	
	// Type of animation in the slider
	// [slide OR fade]
	var animType = 'fade';
	
	// The speed of changing photos in the slider
	var animInterval = 9000;

/* --- Other settings --- */

	// Twitter
	var twitter = {
		// Turn on/off twitter widget
		enb: true,
		
		// username
		name: 'geleyi',
		
		// count of tweets
		count: 5
	}

	// Save page position
	// [true OR false]
	var save_page = false;

	// Color scheme
	var color = {
		// color of countdown
		countdown:  'rgba(255, 255, 255, 0.7)',
		
		// color of links
		links: 		'rgba(240, 0, 0, 0.6)',
		
		// color of hovered links
		linksHover:	'rgba(200, 0, 0, 1)'
	}
	
	// Turn on/off parallax
	var enb_parallax = true;
